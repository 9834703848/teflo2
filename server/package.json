{
  
  "dependencies": {
    "axios": "^0.26.0",
    "express": "^4.17.3",
    "node-fetch": "^3.2.1"
  },
  "scripts": {
    "start": "node app.js",
    "build": "cd ../ && npm install && npm run build"
  },
  "engines": {
    "node": "your-node-version"
  }
}